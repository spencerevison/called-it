# PROGRESS — called-it build log

Format: `T## | gates: pass|fail → blocked | <short-sha> | <one-line note>`
Appended by the author, one line per iteration. Times live in loop/loop.log (real runner clock); the leading timestamps on T01–T10 below were author-guessed and are unreliable. Humans read bottom-up.

---

2026-07-02T19:21:00Z | T01 | gates: pass | 8aaea6b | Next.js 16 + TS strict + Tailwind v4 + shadcn/ui (nova preset); DESIGN.md token set extended onto shadcn defaults; pnpm check = typecheck && lint && vitest run.
2026-07-02T19:38:00Z | T02 | gates: pass | 0bcb964 | GH Actions workflow (.github/workflows/check.yml): pnpm/action-setup v11, node 22, pnpm install --frozen-lockfile && pnpm check on push/PR (no packageManager pin in package.json yet, versions hardcoded in workflow to match local toolchain).
2026-07-02T19:42:00Z | T03 | gates: pass | 5d7b86f | Supabase client helpers (browser+server via @supabase/ssr), .env.example (Supabase/Anthropic/Trigger.dev/Langfuse/Resend), pnpm db:types via supabase CLI devDep; placeholder Database type until T05+ migrations.
2026-07-02T19:47:00Z | T04 | gates: pass | 750e686 | Vitest (T01/T03 already covered example tests) excludes e2e/**; @playwright/test devDep, playwright.config.ts (dev server on :3100), e2e/smoke.spec.ts placeholder, pnpm test:e2e script. Both runners green locally.
2026-07-02T19:53:00Z | T05 | gates: pass | e1ca399 | supabase init (config.toml + supabase/.gitignore); migration 20260702000001_enums_profiles.sql — all DATA_MODEL enums + profiles; verified via supabase start + db reset (docker) applies cleanly to a fresh local db.
2026-07-02T19:58:00Z | T06 | gates: pass | 27d849f | migration 20260702000002_decisions_events.sql — decisions + decision_events tables + indexes per DATA_MODEL; verified via db reset + throwaway psql insert (cascade delete confirmed, remaining_events=0 after parent delete).
2026-07-03T02:51:00Z | T07 | gates: pass | cdf8da5 | migration 20260702000003_forecasts.sql — forecasts table + probability check + resolved/outcome check + indexes; resolved_in_checkin_id FK deferred to T09 per DATA_MODEL comment; verified via db reset + psql (p=0.005 rejected, resolved=true without outcome rejected, valid insert succeeds).
2026-07-03T02:56:00Z | T08 | gates: pass | a29c480 | migration 20260702000004_premortems.sql — premortems + premortem_risks tables, category CHECK (5 values) + indexes; prompt_version FK deferred to T10; verified via db reset + psql (bad category rejected, cascade delete confirmed).
2026-07-03T03:05:00Z | T09 | gates: pass | 04e455e | migration 20260702000005_checkins.sql — checkins + checkin_failures tables + indexes; added deferred forecasts.resolved_in_checkin_id FK; verified via db reset + psql (completed-without-attribution rejected, bogus resolved_in_checkin_id rejected, dup standard-horizon rejected, custom-horizon dups allowed, bogus linked_risk_id rejected, cascade delete confirmed).
2026-07-03T03:12:00Z | T10 | gates: pass | 47cb9ee | migration 20260702000006_prompt_versions_eval.sql — prompt_versions, judge_scores, eval_items, eval_runs tables + judge_scores(decision_id) index; added deferred premortems.prompt_version FK; regenerated src/lib/supabase/types.ts; verified via db reset + psql (bad prompt_version rejected on both premortems and judge_scores insert).
2026-07-03T20:33:00Z | T11 | gates: pass | 6abb6ff | migration 20260702000007_rls.sql — select-own policies on all user tables, prompt_versions read-authenticated, eval tables deny-all (RLS enabled, zero policies); explicit GRANTs (new supabase CLI default no longer auto-exposes tables to PostgREST); service-role client (src/lib/supabase/service.ts); verified via pnpm test:db (db-tests/rls.test.ts, live local supabase, 9 tests: anon/cross-user isolation, no direct writes) — deviations logged in QUESTIONS.md.
T12 | gates: pass | c5bbe37 | scripts/seed.mjs (plain JS, no new devDep — reused .env.local loader pattern from vitest.config.db.ts) via service-role client: 3 committed decisions (1 reversed), 12 forecasts (7 resolved, 2 recalled-probability pairs, 4 desired-resolved, short+long horizon coverage), 2 completed check-ins (1 good/1 bad valence, 4 knowable failures/2 linked); idempotent via delete-by-user_id-then-recreate keyed on a fixed seed email. Header comment documents every hand-computed M1-M10 value. Verified live: db reset + `pnpm db:seed` run twice (second run succeeds cleanly) + psql count check matching every contract minimum exactly.
T13 | gates: pass | c8444d9 | src/lib/metrics/index.ts — all M1-M10 as pure functions per METRICS.md; vectors transcribed verbatim in index.test.ts (17 tests); M6/M9 take pre-partitioned inputs (partition/valence-derivation belongs to T20's aggregation service per METRICS.md's own row-mapping split); M10 vector only gives aggregates so test constructs one decision grouping reproducing them (logged in QUESTIONS.md).
T20 | gates: pass | 524d56a | src/lib/metrics/aggregate.ts — getDashboardMetrics(userId, fetcher) maps DB rows to M1-M10 inputs (horizon partition, checkin valence via resolved_in_checkin_id grouping, reversal-days, knowable-failure join) + gates the metrics with an explicit METRICS.md min-n (M1/M3/M4-desired/M6/M9); src/lib/metrics/supabase-fetcher.ts is the thin live impl (db-tests/aggregate.test.ts, pnpm test:db); src/lib/metrics/aggregate.test.ts is an in-memory fixture mirroring the T12 seed exactly — both equal the seed header hand-computed values, including all four insufficient-data (gated-to-null) cases.
T21 | gates: pass | 8d8ae7d | src/middleware.ts + src/lib/supabase/middleware.ts — deny-by-default route protection (allowlist /login, /auth/confirm), redirects unauthenticated visits to /login?next=<path>; src/lib/safe-next.ts guards the next param via new URL(raw, "http://localhost") origin-compare (unit-tests all four EVIDENCE.md bypass vectors incl. /\evil.com); magic-link sign-in (src/app/login) + /auth/confirm verifyOtp route + sign-out server action; root layout is auth-aware for the sign-out control only, no route group yet (logged in QUESTIONS.md).
T22 | gates: pass | 5aef4d4 | src/app/decisions/{new,[id]/edit}/page.tsx + decision-form.tsx (dynamic options list, min 1, radio-select chosen option kept in sync with live edits) + actions.ts (createDecision/updateDecision server actions, inline validation, service-role writes with ownership+draft-status checks per T11's select-only RLS); errors rendered as an aria-describedby'd list; edit page 404s on non-draft/non-owned via RLS select; no list/detail page yet so create redirects to the new draft's own edit page (logged in QUESTIONS.md).
T23 | gates: pass | f69b461 | src/app/decisions/{forecast-actions.ts,forecast-form.tsx,forecast-list.tsx} — createForecast/updateForecast server actions (ownership+draft-status gate via service-role fetch on the parent decision, same pattern as T22), question/probability(slider+number synced)/desired-checkbox/resolve-by form, wired into the edit page below the decision form; unit-tested via mocked service client routed by table name (decisions vs forecasts).
T24 | gates: pass | 5a581ae | added @anthropic-ai/sdk + langfuse deps; src/lib/prompts/template.ts (mustache-lite loader/renderer for prompts/*.md); src/lib/llm/{client,tracing,premortem}.ts (Anthropic call wrapper gated on ANTHROPIC_API_KEY, Langfuse trace gated on its keys else a local uuid, JSON parse/validate with retry-once-then-error); src/app/decisions/premortem-actions.ts generatePremortem() — ownership+draft+≥2-options gate, persists premortems+premortem_risks(source=ai); fully mocked unit tests (malformed-JSON-then-retry-success, malformed-both-times, no-key-refusal, success path). Live-path FK gap (prompt_versions row for premortem_v1 doesn't exist until T37) logged in QUESTIONS.md.
T24 | review: flag → fix → pass | 4ce3013 | risk:high review surfaced a stack (transactional orphan, TOCTOU, SDK-error escape, template brace-injection); in-loop fixer cleared 2, escalation cap hit; operator-driven fixer finished the last 2 (callAndParse try/catch → ok:false contract; single-pass renderTemplate) + fixed CI frozen-install (pnpm onlyBuiltDependencies). Fable re-review caught a regression in the fix (over-trimmed import), fixed + verified via static import-usage check. See QUESTIONS.md.
T25 | gates: pass | 774903b | src/app/decisions/premortem-panel.tsx — risk list grouped by category with severity badges on the edit page; addUserRisk server action (premortem-actions.ts) ownership-checked via the premortem row (IDOR fix per EVIDENCE.md), draft-only; regenerate wired to T24's generatePremortem. Only the most recent premortem per decision renders (no uniqueness constraint on decision_id, regenerate inserts a new row) — logged in QUESTIONS.md.
T26 | gates: pass | fef7bd7 | migration 20260703000001_commit_decision.sql — commit_decision(decision_id, user_id, 3 dates) plpgsql function, row locked for update, single-transaction (status->active, decided_at, committed event, 3 checkins); execute revoked from anon/authenticated, granted to service_role only (p_user_id is a plain param, not auth.uid()-derived); src/app/decisions/{commit-actions.ts,commit-panel.tsx} call it via service.rpc; wired into edit page with editable default 2w/2m/6m dates. Verified transactionally via db-tests/commit-decision.test.ts against live local supabase (double-commit and non-owner both reject with zero partial writes) + mocked unit tests. Carry-forward TOCTOU note for T24/T25 logged in QUESTIONS.md.
T27 | gates: pass | f743a2b | src/app/decisions/page.tsx (status-filter tabs + list, per-filter empty copy) + src/app/decisions/[id]/page.tsx (read-only detail: entry, forecasts, pre-mortem via PremortemPanel isDraft=false, check-ins) + forecast-timeline.tsx/checkin-timeline.tsx; draft decisions redirect to the existing edit page rather than duplicating the build-up flow. Unresolved forecasts get `probability: null` server-side before render (unit-tested: not in container.textContent, not CSS-hidden) so the recorded value never reaches the DOM per M3. commit-panel now routes to the new detail page instead of home.
T28 | gates: pass | 46a9165 | src/app/decisions/event-actions.ts (revise/reverse/reaffirm server actions, gated to committed (non-draft) decisions, payload.note per DATA_MODEL's own comment) + events-panel.tsx (timeline + inline forms, reverse/revise require a note, reaffirm doesn't) wired into the decision detail page; decision_event_type has no status-changing member so these are pure annotations, matching M8's reversal-event definition — logged in QUESTIONS.md.
T29 | gates: pass | 017158e | added @trigger.dev/sdk@4.5.0 (verified current major); trigger.config.ts (maxDuration 3600) + src/trigger/checkin-reminder.ts (wait.until scheduled_for, then re-fetch+noop-guard via extracted src/lib/checkins/reminder-handler.ts); src/lib/checkins/schedule.ts triggers the reminder per pending check-in and stores trigger_run_id, called from commitDecision right after the RPC (gated on TRIGGER_SECRET_KEY presence, try/catch swallowed — T30's cron is the backstop); fully mocked unit tests for both the handler and the scheduler. Scope call (wiring the trigger into commit, since no later task does it) logged in QUESTIONS.md.
T30 | gates: pass | 838ef2b | src/trigger/checkin-reconcile.ts — schedules.task, cron "0 0 * * *", plain update-where-overdue via extracted src/lib/checkins/reconcile-handler.ts (status='pending' AND scheduled_for < now() -> 'due'); idempotent by construction (re-run matches zero rows once caught up), no run-id/noop-guard needed since it's a bulk WHERE update, not a single-row race like T29's reminder task; mocked unit tests cover the update, the zero-match no-op, and the query-error path.
T31 | gates: pass | fc5665d | src/app/due/page.tsx — RLS-scoped list of status='due' checkins joined to decision title, ordered by scheduled_for, linking to /decisions/[id] (no dedicated checkin route exists yet, T33/T34 not built); root layout header gains a "Due (N)" link using a head-count RLS query, only rendered when signed in. Reused checkin-timeline's HORIZON_LABELS map inline (no shared module to import from without a refactor). No new test file — matches existing precedent (decisions/page.tsx, decisions/[id]/page.tsx) of leaving server-fetched list/detail pages untested at the component level since correctness lives in the RLS/query layer, not local logic.
T33 | gates: pass | 662d266 | src/app/decisions/checkin-actions.ts — getRecallForecasts/submitOutcomeNotes/recordRecall/revealForecast/resolveForecast; recall write-once while revealed_at null, resolve requires prior reveal + same-decision lineage, resolved_in_checkin_id set to this check-in; "can't-resolve-yet" is a no-op on the forecast row (only recall/reveal persist). New /decisions/[id]/checkin/[checkinId] flow page (outcome notes + per-forecast recall→reveal→resolve); due inbox + check-in timeline now link into it (both had nowhere real to send the user before this task) — deviations logged in QUESTIONS.md.
T34 | gates: pass | 697154f | src/app/decisions/checkin-actions.ts — addCheckinFailure (description/linked_risk_id-or-unlisted/was_knowable/attribution, rule-4 cross-decision link rejection via premortem_risks->premortems lookup) + completeCheckin (overall_attribution required, status->completed); both reject once the check-in is already completed; checkin-flow.tsx gains a failures list+form (risk picker sourced from the same latest-premortem convention as T25/T27) and a completion form; deviations logged in QUESTIONS.md.
T35 | gates: pass | 6d2ec4e | supabase/migrations/20260703000002_resolve_decision.sql — resolve_decision(decision_id, user_id, status) RPC mirrors T26's commit_decision shape (row lock, active-only guard, single transaction): sets status+resolved_at, inserts resolved/abandoned terminal event, flips remaining pending/due checkins to skipped so T29/T30's existing CAS guards (status='pending' predicate) naturally keep them from turning due (no changes needed there). src/app/decisions/resolve-actions.ts + resolve-panel.tsx wired into both the decision detail page and the check-in flow page per the AC ("from any check-in or decision page"), gated to render only while status='active'. Verified live via pnpm test:db (5 new tests: resolve, abandon, draft-rejected, non-owner-rejected, double-resolve-rejected) plus mocked unit tests for the action.
T36 | gates: pass | 39a2c18 | e2e/happy-path.spec.ts — full core-flow smoke (sign in -> create decision -> forecast -> mocked pre-mortem -> commit -> force-due -> complete checkin w/ linked+unlisted failure -> resolve), asserts status=resolved on the decision detail page. Anthropic mocked via ANTHROPIC_BASE_URL pointed at a tiny local server (own playwright webServer entry, e2e/mock-anthropic-server.mjs); sign-in via admin.generateLink + the real /auth/confirm route; new E2E_TEST_MODE-gated /api/test/force-due route stands in for the real wait.until wake. Seeded a premortem_v1 prompt_versions row in the spec itself (T37's registry doesn't exist yet) and cleans it up after. Verified green (12.7s) against a scratch copy of the tree since a pre-existing long-running next dev process on this machine blocks a same-directory second instance — deviations logged in QUESTIONS.md.
P5 quality gate | flag → fix → pass | af66857 | terminal check-in state enforced inconsistently — submitOutcomeNotes/resolveForecast lacked the pending/due gate (a forecast resolvable into a completed check-in corrupts M9 valence), and the flow page treated only completed, not skipped (T35), as terminal. Operator-driven fixer added both server gates + gated the flow UI behind a completed||skipped terminal flag + tests. Fable re-review caught a regression in the fix (over-trimmed import), fixed + verified via static import-usage check.
T37 | gates: pass | 5911ab6 | src/lib/prompts/registry.ts — parsePromptHeader (id/kind/notes from the header, sha256 content hash of the whole file) + planPromptRegistration (new id -> insert, matching hash -> skip, changed hash under an existing id -> throws PromptDriftError); unit-tested incl. missing-marker/missing-id/missing-kind and drift cases. scripts/register-prompts.mjs is the thin live script (pnpm db:register-prompts) duplicating the parse logic in plain JS per seed.mjs's precedent (scripts/ isn't run through a TS loader) — reads prompts/*.md, diffs against prompt_versions, inserts new rows, throws loudly on drift. Interpreted "startup/build" as this standalone script rather than a Next.js instrumentation hook (none exists yet) — logged in QUESTIONS.md.
T38 | gates: pass | d882e02 | src/lib/llm/judge.ts (parse/validate judge_v1's strict JSON, retry-once-then-error, hashJudgeInput) + src/app/decisions/judge-actions.ts (assembleJudgeInput selects only the DATA_MODEL rule-3 fields; runJudge persists judge_scores+Langfuse trace, gated on ANTHROPIC_API_KEY, swallowed-on-failure same as T29); wired into commitDecision right after scheduleCheckinReminders. prompts/judge_v1.md already had the `{{#risks}}` block its system prompt refers to (no edit needed there — earlier check misread a broken local grep as an omission). No retry/backstop exists for a missed judge run (unlike T30 backstopping T29) — logged as a scope decision, not built.
T39 | gates: pass | 2875300 | src/app/decisions/[id]/judge-panel.tsx + src/lib/llm/judge-rubric.ts — three judge scores (JUDGE_RUBRIC.md anchors transcribed as static data, shown as a title tooltip on the achieved score), per-dimension rationale, evidence spans; "experimental" badge + one-line explanation shows until new JUDGE_TRUSTED env flag is "true" (flip once eval:judge/T41 clears the EVAL_PLAN §4 bar). No tooltip/shadcn primitive existed in the repo — used native title rather than adding one. Deviations logged in QUESTIONS.md.
P6 quality gate | flag → fix → pass | ebb473b | T37 shipped two copies of the prompt registry — registry.ts (tested but production-dead) and register-prompts.mjs (live, untested) — so the drift guard could silently diverge. Collapsed to one impl: the script imports registry.ts directly (Node 22.18+ TS strip, verified). Also added rubric_version=v1 to the judge Langfuse trace (JUDGE_RUBRIC §Protocol) + a test pinning it. Fable re-review caught a regression in the fix (over-trimmed import), fixed + verified via static import-usage check.
T40 | gates: pass | 41f7cf2 | src/lib/eval/goldset.ts — zod schema for the EVAL_PLAN gold-set entry (unknown keys like `_note` stripped by default z.object() behavior) + parseGoldsetEntry (file-prefixed error message on invalid JSON or a failed field); scripts/eval-import.mjs (pnpm eval:import) reads top-level goldset/*.json, validates all before upserting into eval_items, exits nonzero with every file's error on any failure. Added zod dep. Unit-tested against the real example-001.json plus inline broken-JSON/invalid-field fixtures. Deviation on "fixtures import" logged in QUESTIONS.md.
T41 | gates: pass | b83efb1 | src/lib/eval/{agreement,judge-run}.ts — computeAgreement (within1/exact/mae per JUDGE_DIMENSIONS + macro, hand-computed 3-item fixture test), assembleJudgeInputFromGoldset (risks always [] for gold-set items per JUDGE_RUBRIC's own note), findDisagreements (|diff|>1), renderJudgeReport (aggregate + item ids only, test asserts no title/context/rationale leak); scripts/eval-judge.mjs (pnpm eval:judge --version <v>) live-judges every eval_items row, prints disagreements w/ both rationales to stdout + docs/eval/detail/*.json (gitignored), writes content-free docs/eval/<version>_<date>.md, inserts eval_runs row; refuses to run without ANTHROPIC_API_KEY. Deviations logged in QUESTIONS.md.
T42 | gates: pass | pending-commit | src/lib/eval/premortem-run.ts (buildPremortemPromptContext decision-time-only fields, buildMatchTargets = knowable failures + expected_premortem_risks, computeSurfaceRate M10-style per-failure/per-decision, computeExpectedCoverage, content-free renderPremortemReport) + scripts/eval-premortem.mjs (pnpm eval:premortem --version <v>): generates premortem_v1 per eval_items row via T24's generatePremortemRisks, readline-driven assisted-manual matching against generated risks, persists matches to gitignored docs/eval/detail/premortem_matches_<version>.json keyed (item, target kind+index, version) so a re-run of the same version skips already-answered prompts; writes docs/eval/<version>_premortem_<date>.md + eval_runs row (kind=premortem_surface). Surface-rate unit test pins the METRICS.md M10 vector exactly (0.625/0.8) since EVAL_PLAN explicitly models this on M10's shape.
T43 | gates: pass | pending-commit | src/lib/eval/compare-run.ts (computeCostLatencyStats mean-cost/median-latency, computeDeltaRows, content-free renderCompareReport) + scripts/eval-compare.mjs (pnpm eval:compare --kind premortem v1 v2): pulls the latest eval_runs row per version (from T42's eval:premortem), cost/latency from Langfuse traceList filtered by the run's `run_id:<id>` tag (graceful null stats when Langfuse creds absent, mirrors tracing.ts), writes docs/eval/<kind>_compare_<v1>_vs_<v2>_<date>.md + an eval_runs row (kind=compare). Only "premortem" kind wired up (no second compare-able run type exists yet) — logged in QUESTIONS.md, which also resolves the P6 carry-forward note re: Langfuse metadata casing (T43 filters by tags, not metadata).
T44 | gates: pass | pending-commit | eval-smoke/smoke.test.ts + vitest.config.smoke.ts (`pnpm eval:smoke`, own vitest project excluded from the main config's include net, mirrors vitest.config.db.ts's precedent) + goldset/fixtures/{001,002,003}.json (3 synthetic items, diffs 0/1/3 across all 3 judge dims so agreement math is hand-verifiable); drives import(parseGoldsetEntry)->judge(assembleJudgeInputFromGoldset+generateJudgeScores)->report(computeAgreement/findDisagreements/renderJudgeReport) fully in-memory with `@/lib/llm/client`'s generateText mocked (vi.mock, same pattern as judge.transport.test.ts) — zero live calls, asserted via mock call count; separate `eval-smoke` job added to .github/workflows/check.yml alongside (not inside) the `check` job. Deviation on mock-fetch-vs-mock-client logged in QUESTIONS.md.
P7 quality gate | flag → fix → pass | 4c1048b | eval-harness duplication — CI smoke re-implemented eval-judge's loop (tested≠live, the P6 pattern), the Langfuse tag contract lived inline in 3 places, and the script bootstrap hit 6 copies. Extracted judgeEntries (smoke + CLI share it), evalTraceTags/runIdTag, and scripts/lib/bootstrap.mjs; logged the store-interface deviation. Fable re-review caught a regression in the fix (over-trimmed import), fixed + verified via static import-usage check.
T46 | gates: pass | 4ee95ef | src/app/dashboard/page.tsx — summary cards (decision count, resolved count, open check-ins = pending+due, Brier headline) wired to T20's getDashboardMetrics via the live Supabase fetcher (first live caller — previously only exercised by tests); Brier card shows the min-n insufficient state + its plain-language sentence per METRICS.md's display rule, the count cards are plain numbers (not METRICS.md metrics, no sentence needed). Nav link added to the root layout header. No new test file — matches T31's precedent of leaving server-fetched pages untested at the component level; the underlying aggregation is already covered by T20's seed-fixture test.
T47 | gates: pass | pending-commit | src/lib/metrics/index.ts rollingBrier (M1 trailing-90d window, one point per resolved forecast) + aggregate.ts brierTrend (same min-n=5 gate as brier); src/app/dashboard/charts.tsx — CalibrationChart (diagonal reference line, dot radius by n, n<5 greyed via chart-reference token) + BrierTrendChart, both plain SVG with focusable dot groups + group-focus-within text tooltips (no chart lib in repo); wired into dashboard page below the summary cards. Rolling-window granularity interpretation logged in QUESTIONS.md.
T48 | gates: pass | pending-commit | src/app/dashboard/page.tsx — Bias panel (3 cards: hindsight M3, optimism M4 w/ control value, self-serving M9), each rendering T20's aggregate values via a plain-language sentence + insufficient-data fallback (n/minN per METRICS.md); reused T20's existing gating fields directly, no new metrics logic needed. No new test file, same server-fetched-page precedent as T31/T46.
